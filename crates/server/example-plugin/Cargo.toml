[workspace]

[package]
name = "example-plugin"
version = "0.1.0"
edition = "2024"

[lib]
name = "init_handler"
crate-type = [ "cdylib" ]

[dependencies]
server = { package = "http-srv", path = "../" }
